# ==========================================
# Flask Environment Configuration Template
# ==========================================
# 1. Copy this file and rename it to .env
# 2. Update the values below with your specific settings
# 3. Never commit the .env file to Git!
# ==========================================

# --- Flask Settings ---
# Entry point for the application
FLASK_APP=run.py

# Enable debug mode for development (1=On, 0=Off)
FLASK_DEBUG=1

# --- Security ---
# Crucial for sessions and CSRF protection.
# You can generate a strong key in your terminal via: 
# python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=replace-with-a-secure-random-string

# --- Database ---
# Connection string for SQLAlchemy.
# For SQLite (default): sqlite:///app.db (Files are stored in /instance folder)
# For PostgreSQL: postgresql://user:password@localhost:5432/dbname
SQLALCHEMY_DATABASE_URI=sqlite:///app.db